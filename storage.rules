rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /charts/{allPaths=**} {
      // Storage rules can't cross-reference Firestore, so we just require auth.
      // The app itself is gated by the Firestore allowedUsers check.
      allow read, write: if request.auth != null;
    }
  }
}
